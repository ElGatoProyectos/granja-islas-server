export const companyMulterProperties = {
  folder: "companies",
  field: "company-profile",
};

export const userMulterProperties = {
  folder: "users",
  field: "user-profile",
};

export const voucherMulterProperties = {
  folder: "vouchers",
  field: "voucher",
};
