class CryptoService {
  decryptCredentialasCompany = async (
    client_id: string,
    client_secret: string
  ) => {};

  encryptCredentialasCompany = async (
    client_id: string,
    client_secret: string
  ) => {};
}

export default CryptoService;
